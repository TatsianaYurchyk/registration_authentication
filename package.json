{"name": "mern",
  "version": "1.0.0",
  "description": "task4",
  "scripts": {
    "build": "cd backend && npm install  && cd ../frontend && npm install && npm run build",
    "start": "node backend/src/server.ts"
}


}